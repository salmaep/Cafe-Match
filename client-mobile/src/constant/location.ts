// Fallback location used by LocationContext when GPS is denied or fails.
// Jakarta Selatan area — broad enough to give meaningful results from server search.
export const DEFAULT_LOCATION: { latitude: number; longitude: number } = {
  latitude: -6.2615,
  longitude: 106.8106,
};
